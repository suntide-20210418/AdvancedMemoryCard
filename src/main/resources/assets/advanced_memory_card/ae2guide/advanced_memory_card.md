---
navigation:
  parent: ae2:items-blocks-machines/items-blocks-machines-index.md
  title: 高级内存卡
  icon: advanced_memory_card:advanced_memory_card
categories:
 - tools
item_ids:
 - advanced_memory_card:advanced_memory_card
---

# 高级内存卡

<ItemImage id="advanced_memory_card:advanced_memory_card" scale="4" />

高级内存卡是标准[内存卡](ae2:items-blocks-machines/memory_card.md)的增强版本，为Applied Energistics 2系统提供强大的自动化配置能力。它能够简化重复性的配置任务，并实现设备设置的高效批量部署。

与标准内存卡不同，高级内存卡具备**复制模式**和**配置模式**两种操作模式，使大规模AE2网络管理变得更加高效。

## 合成配方

<RecipeFor id="advanced_memory_card:advanced_memory_card" />

将1个AE2内存卡与逻辑处理器、计算处理器、工程处理器各一个无序合成即可获得。

## 基本操作

- 手持高级内存卡时，按 **V 键**（可在按键设置中修改）在复制模式和配置模式之间切换
- 物品提示栏会显示当前所处的模式及状态信息
- 复制模式下，**右键点击空气** 执行批量粘贴操作

---

# 复制模式

复制模式允许您将一个设备的配置批量复制到指定矩形区域内的所有兼容设备上，大幅提升大规模AE2网络部署的效率。

## 使用流程

1. **Shift + 右键** 点击一个AE2设备，复制其配置设置（与普通内存卡相同）
2. **右键点击** 方块的第一个角落，标记为区域起始点
3. **右键点击** 对角线的另一个角落，标记为区域结束点
4. **右键点击空气**，将存储的配置批量粘贴到选定区域内所有兼容设备

## 功能特点

- 支持选择最大 **2048个方块** 体积的区域
- 适用于所有AE2方块实体（ME接口、存储总线、输出总线等）
- 兼容安装在线缆上的AE2部件（破坏平面、成型平面、P2P通道等）
- 选择区域过大时自动提示错误并重置选择
- 粘贴完成后自动清除选择状态

## 视觉指示器

选择区域会以彩色线框实时渲染在世界中：
- **红色框**：已选择起始点，等待选择结束点
- **白色框**：两个点均已选择，区域已定义
- **绿色框**：区域已确认，右键点击空气即可粘贴
- 选择过程中，准星指向的方块会显示绿色高亮框

---

# 配置模式（P2P 隧道管理）

配置模式是高级内存卡的强大P2P隧道集中管理工具。它能够扫描AE2网络中所有的P2P设备，并提供可视化的管理界面。

## 打开配置模式

手持卡片在配置模式下，**右键点击** AE2网络中的任意P2P隧道部件，即可打开P2P管理GUI。

## GUI 界面布局

P2P配置模式界面分为三个主要区域：

### 左侧：P2P树形控件
- 按 **P2P类型 → 频段 → P2P设备** 的三层树形结构展示所有P2P设备
- 每个P2P设备前有彩色圆点指示其连接状态：
  - **绿色圆点**：已连接且激活
  - **红色圆点**：未连接
  - **灰色圆点**：未激活
- 蓝色箭头图标标记输入端，黄色箭头图标标记输出端
- 紫色加粗文字标记待绑定的P2P设备
- 顶部搜索框支持按类型、频段、名称、别名搜索P2P设备

### 右侧：详情面板
根据选中节点的类型显示不同的信息和操作按钮：

**P2P设备节点**：
- 显示名称（可编辑）、频段、类型、维度、坐标、方向、连接状态
- 对于ME P2P，额外显示频道使用情况（已用/剩余/总数）
- 操作按钮：改名、选择（设为待绑定）、高亮、定位、分配新频段、刷新

**频段节点**：
- 显示频段别名（可编辑）、P2P数量、频道信息
- 输入/输出端统计
- 操作按钮：改名、绑定（将待绑定的P2P设备绑定到此频段）、高亮、定位、刷新

**P2P类型节点**：
- 显示类型名称、P2P总数、频段总数
- 对于ME P2P类型，提供"初始化P2P"按钮，自动配置所有未分配频段的ME P2P设备的输入/输出角色

### 底部：提示信息
- 显示当前操作提示，如"选择一个 P2P 节点查看详情"

## 核心功能

### P2P 设备绑定
1. 在树形控件中选择一个未绑定的P2P设备
2. 点击"选择"按钮将其标记为待绑定
3. 选择目标频段节点
4. 点击"绑定"按钮完成绑定

### 自动配置 ME P2P I/O
点击P2P类型节点中的"初始化P2P"按钮，系统会自动：
- 检测每个ME P2P所在网络是否有ME控制器
  - 有控制器 → 设置为输入端
  - 无控制器 → 设置为输出端
- 为未分配频段的P2P自动分配新频段

### P2P 高亮与传送
- 点击"高亮"按钮后，GUI会关闭并在世界中以彩色线框渲染目标设备（持续5秒）
- 红色框：当前目标设备
- 绿色框：该频段的输入端
- 蓝色框：该频段的输出端
- 同时聊天栏会输出可点击的传送命令，点击即可传送到设备位置（需要作弊权限）

### 重命名
- P2P设备节点：可修改设备名称，方便在大规模网络中识别
- 频段节点：可设置频段别名（实质为修改该频段输入端P2P的名称）

### 分配新频段
对于非ME类型的P2P设备，点击"分配新频段"按钮会：
- 断开当前连接
- 将设备设为输入端
- 分配一个新的唯一频段

---

# 兼容设备

## 复制模式兼容
所有支持AE2内存卡设置的设备和部件，包括但不限于：
- ME接口 (ME Interface)
- 存储总线 (Storage Bus)
- 输入/输出总线 (Import/Export Bus)
- 液位发射器 (Level Emitter)
- 能源接收器 (Energy Acceptor)
- 破坏/成型平面 (Annihilation/Formation Plane)
- 分子装配室 (Molecular Assembler)
- P2P通道 (P2P Tunnel)

## 配置模式兼容
AE2网络中所有类型的P2P隧道部件：
- ME P2P通道
- 物品 P2P通道
- 流体 P2P通道
- 红石 P2P通道
- 能源 P2P通道
- 光 P2P通道
- 以及所有其他P2P类型

---

此工具显著减少了大规模AE2网络设置、维护和P2P管理所需的时间和精力。
